This project is built on express and firebase.
with the crud, RESTful api, typescript.

Create an `env` folder and put `serviceKey.json` into it, this file is the token on the firebase admin.

## How to Run?
```js
> npm install
> npm run start
```
